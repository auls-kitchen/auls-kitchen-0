{
  "name": "aul-world-runtime",
  "version": "0.0.1",
  "private": true,
  "description": "AWR-01 isolated prototype — AUL World Runtime architecture/engine proof. Not wired into production. Not deployed.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -p tsconfig.json --noEmit && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "pixi.js": "^8.6.6"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vite": "^5.4.11"
  }
}
