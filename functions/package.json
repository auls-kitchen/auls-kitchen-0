{
  "name": "aul-kitchen-functions",
  "description": "AUL's Kitchen — Cloud Functions trusted boundary (Scope 16: healthCheck only)",
  "main": "index.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "lint": "true",
    "serve": "firebase emulators:start --only functions",
    "shell": "firebase functions:shell",
    "start": "npm run shell",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^5.0.0"
  },
  "private": true
}
